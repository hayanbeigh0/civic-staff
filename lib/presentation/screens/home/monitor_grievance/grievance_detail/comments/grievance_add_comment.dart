import 'package:flutter/material.dart';

class GrievanceAddComment extends StatelessWidget {
  static const routeName = '/grievanceAddComment';
  const GrievanceAddComment({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
